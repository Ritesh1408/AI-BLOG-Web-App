version: '3.8'

services:
  frontend:
    build: ./frontend
    command: npm run dev
    ports:
      - "80:5173"
    depends_on:
      - backend
    environment:
      - HOST=0.0.0.0
    restart: always

  backend:
    build: ./backend
    command: npm run dev
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - HOST=0.0.0.0
    restart: always
